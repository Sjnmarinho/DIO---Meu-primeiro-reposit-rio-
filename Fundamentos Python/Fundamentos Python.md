# 🐍 Variáveis em Python

- Variáveis são usadas para armazenar valores na memória.
- Não é necessário declarar o tipo, Python faz isso automaticamente.

## Exemplos:

```python
nome = "Maria"
idade = 25
altura = 1.68
estudando = True

print(nome, idade, altura, estudando)